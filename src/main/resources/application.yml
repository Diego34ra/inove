spring:
  application:
    name: inove
  profiles:
    active: local   # profile padrão
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  api:
    security:
      token:
        expiration: 1
        secret: ${JWT_SECRET:my-secret-key}
      refresh-token:
        expiration: 8

springdoc:
  swagger-ui:
    tags-sorter: method
    operations-sorter: alpha



cloud:
  aws:
    credentials:
      accessKey: ${AWS_ACCESS_KEY}
      secretKey: ${AWS_SECRET_KEY}
    region:
      static: us-east-1
    s3:
      bucket: inove-bucket-streaming

mail:
  host: smtp.gmail.com
  port: 587
  username: inoveprojetointegrador@gmail.com
  password: ${MAIL_PASSWORD:jwhg nbfe iiob lwvz }
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true

admin:
  email: inoveprojetointegrador@gmail.com
